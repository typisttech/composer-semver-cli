name: Publish

on:
  release:
    types:
      - published

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

permissions: {}

jobs:
  cloudsmith:
    runs-on: ubuntu-latest
    steps:
      - run: ${{ tojson(github) }}
        shell: cat {0}

      - run: gh release download --repo "${REPO}" --pattern '*.deb' "${TAG}"
        env:
          REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
          GITHUB_TOKEN: ${{ github.token }}

      # TODO:
      - run: ls -R .

      - run: find -type f -name '*.deb' . | xargs -I {} gh release upload ${CLOBBER} --repo "${REPO}" "${TAG}" {}
      - run: gh attestation verify --repo "${REPO}" "${FILE}"
        env:
          REPO: ${{ github.repository }}
          FILE: composer-semver_linux_arm64.deb
          GITHUB_TOKEN: ${{ github.token }}
