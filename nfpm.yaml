# yaml-language-server: $schema=https://nfpm.goreleaser.com/static/schema.json

name: "composer-semver"

arch: ${GOARCH}
version: ${SEMVER}

maintainer: "Typist Tech Limited <opensource+composer-semver@typist.tech>"
description: |
  TODO!
homepage: "https://github.com/typisttech/composer-semver-cli"
license: "MIT"

contents:
  - src: ./composer-semver_linux_${GOARCH}/composer-semver
    dst: /usr/bin/composer-semver
    expand: true
